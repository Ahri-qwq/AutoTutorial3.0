# 风格审查报告

## 6.1 AI腔检测

执行：`grep -n "在当今|随着.*的发展|值得注意的是|综上所述|不可忽视|越来越|日益|至关重要" *.md`

**结果：无匹配项** ✅ 无 AI 腔表达。

## 6.2 长句检测

通过 grep 检测长于 90 字符的段落文字行（排除标题、表格、代码、URL）：

**发现2处需要拆分的长句：**

**问题1**（03_chapter_01，line 15）：
"有限位移方法（Finite Displacement Method）的物理思路：对超胞中的某个原子施加一个微小位移 **u**，计算其他原子受到的恢复力 **F**。在谐近似下，力与位移的关系由力常数矩阵 Φ 连接："

→ 拆分为两句：先说思路，再说公式

**问题2**（03_chapter_01，line 25）：
"**为什么需要扩胞？** 力常数矩阵描述的是原子间的相互作用，需要足够大的超胞才能保证原子间相互作用的衰减，从而使截断误差可接受。通常要求扩胞后三个方向的晶格长度均在 10–20 Å 之间。"

→ 第一句可拆为两句

**第三章的长句（均为加粗要点+说明的格式）**：可接受，是参考文章的常见风格。

## 6.3 风格对齐

对照参考文章风格检查：
- ✅ 使用一、二、三 中文数字节序
- ✅ 代码块使用反引号，格式规范
- ✅ 无空洞的背景介绍
- ✅ 参数说明使用表格
- ✅ 前言简洁直接

## 修改后内容

**修改1（chapter_01，line 15）：**

原文：
```
有限位移方法（Finite Displacement Method）的物理思路：对超胞中的某个原子施加一个微小位移 **u**，计算其他原子受到的恢复力 **F**。在谐近似下，力与位移的关系由力常数矩阵 Φ 连接：
```
改为：
```
有限位移方法（Finite Displacement Method）的核心操作：对超胞中的某个原子施加微小位移 **u**，然后计算其他原子受到的恢复力 **F**。在谐近似下，力与位移通过力常数矩阵 Φ 线性关联：
```

**修改2（chapter_01，line 25）：**

原文：
```
**为什么需要扩胞？** 力常数矩阵描述的是原子间的相互作用，需要足够大的超胞才能保证原子间相互作用的衰减，从而使截断误差可接受。通常要求扩胞后三个方向的晶格长度均在 10–20 Å 之间。
```
改为：
```
**为什么需要扩胞？** 力常数矩阵描述原子间的相互作用，距离越远影响越小。超胞需要足够大，才能让边界以外的相互作用衰减到可以忽略的程度。通常要求扩胞后三个方向的晶格长度均在 10–20 Å 之间。
```

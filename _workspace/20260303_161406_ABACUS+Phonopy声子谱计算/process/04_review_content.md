# 内容审查报告

## 逻辑审查

**通过项：**
- 章节顺序：前言 → 原理 → 案例步骤 → 结果分析 → 附录，逻辑连贯
- 计算流程步骤完整：准备结构 → 生成微扰 → 计算受力 → 生成FORCE_SETS → 声子谱 → 绘图
- 无前后矛盾

**问题：**
- 扩胞大小讨论在 2.3 节和 3.2 节均有出现，轻微重复（2.3节的扩胞讨论移至3.2节，2.3只保留操作层面说明）

## 事实审查

**正确项（对照案例逐一核查）：**
- STRU 文件内容：与案例完全一致 ✓
- INPUT 文件内容：与案例完全一致 ✓
- band.conf 文件内容：与案例完全一致 ✓
- plot_pho.gp 文件内容：与案例完全一致 ✓
- phonopy -d 命令：`phonopy -d --dim="2 2 2" --abacus` ✓
- phonopy 绘图命令：`phonopy-bandplot --gnuplot > pho.dat` ✓

**问题1（已发现）：phonopy -f 命令路径不一致**
- 我的草稿：`phonopy -f ./OUT.Al-fcc/running_scf.log`
- 案例文件：`phonopy -f ./disp-001/OUT*/running_scf.log ...`
- ABACUS官方文档：`phonopy -f ./disp-{number}/OUT*/running*.log`
- **修正：** 改为与案例一致的 `disp-001/` 子目录方式，并在 2.4 节说明需要在子目录中运行 ABACUS

**问题2：m 0 0 0 解释**
- 草稿："三个方向均不固定（允许受力计算时原子受约束为0位移...）"
- 修正："0 表示该方向不约束（原子可以自由移动），m 0 0 0 表示原子在三个方向均不受约束"

**问题3：缺少 KPT 文件提示**
- ABACUS LCAO 计算需要 KPT 文件（k 点网格），案例文件中未显示其内容，但目录中应有 KPT 文件
- **修正：** 在目录结构中补充 KPT 文件，并简要说明

## 结构审查

- 前言：约17行，合理
- 第一章（原理）：约40行，略短但内容完整，合理
- 第二章（案例步骤）：约320行，主体内容，合理
- 第三章（结果+注意事项）：约45行，合理
- 附录：约42行，合理
- **总行数：460行**，在参考文章0.7-4倍范围（240-1380行）内

## 修改计划

1. 2.3节：删除扩胞大小详细讨论，改为"推荐10-20 Å，详见第三章"
2. 2.4节：将"运行ABACUS"的说明改为在 disp-001/ 子目录中运行，与案例 phonopy -f 命令格式一致
3. 2.5节：更新 phonopy -f 命令为 `./disp-001/OUT.Al-fcc/running_scf.log`
4. STRU说明：修正 `m 0 0 0` 的解释
5. 目录结构：补充 KPT 文件，并说明

---

## 修改后的稿件

（见 04_review_content.md 末尾的修订版本，下方仅标注修改位置）

### 修改一：补充 KPT 文件到目录结构（2.1节）

**原文：**
```
1_Al/
├── STRU
├── INPUT
└── psp/
    ├── Al_ONCV_PBE-1.0.upf
    └── Al_gga_7au_100Ry_4s4p1d.orb
```

**改为：**
```
1_Al/
├── STRU                    # FCC Al 初始结构
├── INPUT                   # ABACUS 计算参数（SCF + 受力计算）
├── KPT                     # k 点设置
└── psp/
    ├── Al_ONCV_PBE-1.0.upf   # Al 赝势
    └── Al_gga_7au_100Ry_4s4p1d.orb  # Al 数值原子轨道
```

### 修改二：`m 0 0 0` 解释（2.2节）

**原文：** `m 0 0 0` 表示三个方向均不固定（允许受力计算时原子受约束为0位移...）

**改为：** `m 0 0 0` 中，`m` 是关键字，后面三个数字表示各方向的约束：`0` 表示不约束（原子可自由移动），`1` 表示固定。`0 0 0` 即三个方向均不约束。

### 修改三：2.3节删除扩胞大小详细讨论

删除最后一段"扩胞大小的选取..."，改为一句话引用第三章。

### 修改四：2.4节添加子目录说明，并更新 phonopy -f 命令

在"提交计算"部分，增加说明：将输入文件复制到 `disp-001/` 目录中运行 ABACUS，保持与官方案例的目录组织一致。

### 修改五：2.5节更新命令

**原文：** `phonopy -f ./OUT.Al-fcc/running_scf.log`

**改为：** `phonopy -f ./disp-001/OUT.Al-fcc/running_scf.log`

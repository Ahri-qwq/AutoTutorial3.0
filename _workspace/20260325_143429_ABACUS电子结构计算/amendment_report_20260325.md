# 修改报告

**日期：** 2026-03-25
**修改人：** AutoTutorial 3.0
**目标文件：** `07_Final_Tutorial_ABACUS电子结构计算_能带与态密度.md`

---

## 修改背景

在教程中加入一段使用 abacustest 对 DOS/PDOS 进行作图的介绍，并提供了参考文件 `docs/case/力学_电子结构/abacustest.md`（`abacustest model dos-pdos -h` 的输出）。

---

## 涉及教程范围判断

共检查3篇最新教程：

| 教程 | 是否含 DOS/PDOS 实操内容 | 是否修改 |
|------|------------------------|--------|
| ABACUS输入输出体系三文件入门与收敛性测试 | 否（仅一句话提及） | 不修改 |
| ABACUS力学性质计算：结构优化与弹性常数 | 否 | 不修改 |
| ABACUS电子结构计算_能带与态密度 | **是**（第三章完整实操） | **修改** |

---

## 修改内容

**插入位置：** 第三章 3.5 节末尾（原 `---` 分隔线之前）、3.6 节之前

**新增小节：** `### 3.5b 使用 abacustest 绘制 DOS/PDOS`

**新增行数：** 约 25 行

**内容来源：** 完全基于提供的 `abacustest model dos-pdos -h` 帮助文本，包括：
- 命令名 `abacustest model dos-pdos`
- `-j` 参数：指向 ABACUS 计算输入目录
- `--plot-type` 四种模式：`species`、`shell`、`orbital`、`atom`
- `--range` 默认能量范围 `-10 10`（相对费米能，eV）
- `--suffix` 输出文件命名控制

**与教程原有内容的衔接：**
- 作为 Atomkit `abacus-plot` 方法的**替代方案**并列呈现，不替换原有内容
- 保留"PDOS 需要 LCAO 基组"的一致性提示，与 3.5 节保持连贯

---

## 修改前后对比

**修改前结构：**
```
### 3.5 PDOS 绘图（需切换 LCAO 基组）
---
### 3.6 结果解读
```

**修改后结构：**
```
### 3.5 PDOS 绘图（需切换 LCAO 基组）
---
### 3.5b 使用 abacustest 绘制 DOS/PDOS   ← 新增
---
### 3.6 结果解读
```
